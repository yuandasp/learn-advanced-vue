<script setup>
import { defineProps } from "vue";

defineProps({
  bg: { type: String, default: "bg-gray-100" },
});
</script>

<template>
  <div :class="`${bg} p-6 rounded-lg shadow-md`">
    <!-- kalau di react, slot seperti {children} -->
    <slot></slot>
  </div>
</template>
